import styled from "styled-components";

export const main = styled.main`
  min-width: 65%;
  max-width: 65%;

  display: flex;
  flex-wrap: wrap;
  gap: 1.3rem;

  overflow: hidden;

  > img {
    width: 48%;
  }
`;
